<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class ProductSeeder extends Seeder
{
    /**
     * Products are created via Admin / Merchant panels with real uploads.
     * Demo catalog seeding is disabled to avoid mismatched placeholder images.
     */
    public function run(): void
    {
        // Intentionally empty — add products from Admin → Products.
    }
}
